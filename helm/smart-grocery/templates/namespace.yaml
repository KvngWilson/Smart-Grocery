apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.global.namespace }}
  labels:
    name: {{ .Values.global.namespace }}
    app.kubernetes.io/name: {{ include "smart-grocery.name" . }}
    helm.sh/chart: {{ include "smart-grocery.chart" . }}
